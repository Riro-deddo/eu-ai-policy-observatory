# Read-only candidate integration review

Reviewer: AI-assisted evidence review. Reviewed through 2026-09-06T23:10:33Z. Baseline: `39c715948ee67d7071383c02b813d85c13b64c12`. Scope: 26 added documents and their dependencies, 34 candidate decisions and prior histories, preservation of the 192 earlier document files, and integrated evidence/report consistency. The four retained-record holds and the known stale-count test repairs were excluded because they are assigned separately. No canonical changes were made.

## Material findings

**Follow-up on cross-platform testing — P1:** `tests/test_candidate_snapshot.py::test_original_192_documents_remain_unmodified` compares raw working-tree bytes with Windows CRLF baseline hashes. The repository has `core.autocrlf=true` locally and no applicable `text`/`eol` override. Linux CI normally checks out the Git LF blobs, so this new regression fails despite identical records. Confirmed on `artificial-intelligence-act.json`: local 125 CRLFs, raw SHA-256 `76b0025277c7b5259f62079df6cb13d1098b25ab93d836e6b14fdba5c56e8174`; Git blob zero CRLFs, SHA-256 `cbb98dc176f64058368cd800457e0e571078826eaf7f37d9910bcb6061840412`. Normalising only CRLF to LF makes local bytes exactly equal the Git blob. Retain each raw `sha256` as its actual local receipt; add a separately labelled `lf_sha256` computed from the baseline Git blob or verified unchanged local bytes and compare the test input after the same CRLF-to-LF normalisation. Do not overwrite the true raw receipt with a normalised hash under the same meaning. The parent was notified and is addressing this.

1. **P2 — final ledger drops two concrete reopening conditions.** `research/reviews/2026-09-06-candidate-snapshot/candidate-decisions.json` has `reopen_when: null` for `eba-ai-act-mapping-letter-2025` and `ecb-ai-financial-stability-special-feature-2024`. The earlier `health-finance/decisions.json` supplies explicit `reopening_criterion` values, and the final prose summary describes them. Copy those conditions into the final ledger's field so the declared authoritative decision record preserves what evidence would resolve each hold. This appears to be a field-name translation omission, not an evidence disagreement.

2. **P2 — copied health/finance canonical links are broken, including a link to an unadmitted record.** All six `[JSON]` links in `research/reviews/2026-09-06-candidate-snapshot/health-finance/report.md` use `../../../../../data/documents/...`, which escapes one level beyond the repository. The five admitted documents require `../../../../data/documents/...`. The Annex 22 document does not exist canonically and must not retain a canonical link; replace it with an explicit historical draft/overridden-recommendation note or a genuine retained draft artefact link. The top-level override is clear, but it cannot make these links resolve.

3. **P2 — legacy report describes a repository-relative capture directory that was not integrated.** `research/reviews/2026-09-06-candidate-snapshot/legacy/report.md` says complete decoded API responses are retained under `captures/`, and `legacy/decision-ledger.json` points to `captures/CJ40-AM-...-api.json` and receipt captures. The integrated `legacy/captures/` directory is absent; the actual evidence is only in the outer local `audit-reports/candidate-legacy-twelve-20260906/captures/` package. Before distributing the citable snapshot, either retain the permitted evidence/extracts at the advertised location, with precise hash and extraction qualifications, or revise these statements/paths to say explicitly which evidence remains local-only. Do not claim the snapshot contains reconstructable response bodies when it does not. This does not require committing full PDFs or multilingual raw responses against repository policy.

The parent fixed findings 1 and 2 during review: an independent follow-up found no missing pending reopening conditions and no broken health/finance Markdown links. Finding 3 and the cross-platform test change were still being addressed when reported. Historical group reports may remain intact if their local-only artefact context is explicitly qualified by the integrated report; do not invent repository copies.

## Confirmed integration properties

- Exactly 26 new document IDs are present, yielding 218 documents. All 192 earlier IDs remain. Their file hashes match every value in the recorded local baseline, and Git reports no tracked document changes against the supplied commit. A raw Git-blob comparison initially exposed expected LF/CRLF checkout differences; those were not mistaken for content edits.
- Exactly the prior 34 pending inventory candidates were reviewed. Each retains the exact six-field prior decision snapshot in `decision_history`. No previously non-pending inventory entry changed, and no new candidate IDs were added. The final ledger contains 34 entries and correctly distinguishes the Annex 22 override from the earlier group recommendation.
- All 26 new documents' declared sources, institutions, policies and concepts resolve to published dependencies. The nine Parliament procedural links target the existing draft report; the JRC second edition has an evidenced revision relationship to the first edition. No additional required parent relationship was identified among the 26 admitted records. Annex 22 remains outside canonical data.
- For all nine new Parliament compilations, independently parsed the captured official API response's English PDF manifestation, checked its issuance date against the canonical publication date, checked version 0100 and the draft-report endpoint, and compared the retained original PDF's byte count and SHA-256 against the API and canonical snapshot. All nine passed. Each retained cover also prints 13 June 2022. The Work-level 10 June date remains distinct and is not used as release evidence.
- The seven root-owned JRC/legal records and five sector records were checked in earlier independent passes during this same integration work. The accepted JRC first-edition earlier-publication finding is now addressed structurally with its official source. The 2022 Council Decision keeps its explicit conditional-expiry qualification. The five admitted health/finance records retain their reviewed exact dates and distinct institutional roles; the rejected Annex 22 interpretation is clearly superseded by the final outcome.
- Counts in the consolidated first-stage summary agree with the integration: legacy 9 included/3 pending; health/finance 5/3; sector 5/1; root 7/1; total 26/8. Earlier group reports are explicitly labelled as superseded recommendations rather than quietly rewritten into a different review history.

## Limits

This is a source-backed integration check, not a fresh institutional sweep, an exhaustive sponsor/concept annotation, an independent review of the four retained holds, or final publication approval. Existing-source publication statuses were checked as dependencies; their earlier evidence was not reopened. Full-suite and generated-output checks are the parent's responsibility. The helper `integration-check.py` is an external read-only diagnostic; its naive raw Git-blob byte comparison is not the preservation verdict, which uses the recorded local baseline hashes and Git's tracked diff as explained above.

## Integration resolution

The parent fixed all reported findings: separate LF hashes now preserve cross-platform comparison without changing raw receipts; all eight pending entries have reopening conditions; canonical report links resolve and Annex22 has no false canonical link; the legacy report explicitly identifies captures and drafts as local-only. The initial review above is retained as submitted. All192 original working-file hashes were checked unchanged before LF-hash derivation. Full Python suite subsequently passed516 tests with1 skipped; Node suite passed65 tests. Full rendered CI remains a separate merge gate.
