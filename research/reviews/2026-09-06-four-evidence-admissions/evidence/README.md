# Evidence archive

These deterministic gzip files preserve the exact official-source bytes cited by the four-record admission ledger. Each archive uses a zero gzip modification time; the ledger records the original retrieval timestamp and SHA-256 of the decompressed source.
