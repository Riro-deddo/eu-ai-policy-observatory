# ST 10069 evidence admission implementation plan

> **For agentic workers:** Use superpowers:executing-plans for inline execution, preserving the user's approved current checkout.

**Goal:** Admit ST 10069 after its dated-access gap was resolved and narrow the four remaining hold explanations.

**Architecture:** Data-only update through existing schema, readiness and compatibility gates. No new field, relationship rule, frontend behavior or source exception.

**Tech Stack:** Canonical JSON, Python validators and SQLite pipeline, Astro static build.

**Spec:** `../../../../evidence-five-deep-20260906/audit.md` and `docs/historical-readiness.md`.

## Global constraints

English repository text; cutoff 2026-09-04; 187 stable routes; only ST 10069 upgraded; exact existing reviewer credits preserved; no Git write or deployment. Current checkout is explicitly user-approved despite old Git history; byte snapshot is the preservation baseline.

## Task 1: Evidence-backed admission and regression checks

Files: `data/documents/ai-act-council-first-consolidated-compromise-st-10069-2022.json`, new Council request and guide sources in `data/sources/`, `tests/test_st10069_evidence_admission.py`, current count expectations in the six/seven batch tests.

- [x] Snapshot approved files and pass the existing seven-admission baseline tests (10 passed).
- [x] Add tests requiring document issue 2022-06-15, documented access 2022-06-20, Council Presidency attribution, multilingual source URL, exact 183/4 counts and preserved lineage.
- [x] Run `pytest tests/test_st10069_evidence_admission.py -q` before data edits; expect missing extension/date/count failures.
- [x] Add verified official access-request sources 22/1313 and 22/1307 plus dataset-semantics guide. Complete all required date, role, classification, author and lineage evidence. Qualify disclosure as not first-ever publication.
- [x] Run focused tests, candidate readiness and the production publication gate; expect zero candidate/publication issues.

## Task 2: Narrow holds and deliver checked local outputs

Files: four held document notes, one recovered July source; new `research/migrations/2026-09-06-st10069-evidence-admission.json`; append current checkpoint in `docs/historical-readiness.md`. Existing historical ledgers remain unchanged.

- [x] Link the official July ANNEX to both GPAI records and explicitly retain their parent-publication hold, without partial extension blocks.
- [x] Explain ST 12206's exact identity conflict and C(2025)3871's publication-day gap. Do not silently alter unresolved metadata or statuses.
- [x] Run `pytest -q` with a short temporary base path, then the existing pipeline and SQLite integrity/foreign-key checks; expect 187 documents, 183 verified, four pending and 114 published relationships.
- [ ] Build the unchanged frontend against updated public JSON and run the public-build checker. Record any unavailable check without claiming success.
- [x] Run `../evidence-st10069-admission-20260906/check.py verify`; preserve all 182 previously verified document bytes, route IDs, old ledgers, schema and production code.
- [x] Report local data completion and explicit non-deployment. Commit/push is excluded from this task; website verification remains blocked as recorded below.

## Validation checkpoint

Data admission and preservation gates passed: 464 Python tests passed, one skipped; candidate readiness zero issues; fresh isolated pipeline and SQLite integrity passed at 187/183/4 and 114 published relationships. Existing repository generated outputs remain at 182/5; use the fresh pair in `../evidence-st10069-admission-20260906/generated/` for this batch. Frontend build remains blocked by EPERM at `web/.astro/collections` despite a scoped permission grant. No deployment, code workaround, or false successful-build claim.
